import { MentionuserdataDirective } from './mentionuserdata.directive';

describe('MentionuserdataDirective', () => {
  it('should create an instance', () => {
    const directive = new MentionuserdataDirective();
    expect(directive).toBeTruthy();
  });
});
