import { DatepairDirective } from './datepair.directive';

describe('DatepairDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepairDirective();
    expect(directive).toBeTruthy();
  });
});
