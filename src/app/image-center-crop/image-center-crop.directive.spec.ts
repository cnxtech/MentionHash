import { ImageCenterCropDirective } from './image-center-crop.directive';

describe('ImageCenterCropDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageCenterCropDirective();
    expect(directive).toBeTruthy();
  });
});
