import { MyInhertLinkDirective } from './my-inhert-link.directive';

describe('MyInhertLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new MyInhertLinkDirective();
    expect(directive).toBeTruthy();
  });
});
