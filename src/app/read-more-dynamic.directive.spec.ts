import { ReadMoreDynamicDirective } from './read-more-dynamic.directive';

describe('ReadMoreDynamicDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadMoreDynamicDirective();
    expect(directive).toBeTruthy();
  });
});
