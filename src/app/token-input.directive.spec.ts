import { TokenInputDirective } from './token-input.directive';

describe('TokenInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TokenInputDirective();
    expect(directive).toBeTruthy();
  });
});
